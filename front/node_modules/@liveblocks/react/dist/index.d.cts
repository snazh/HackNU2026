import { R as RegisterAiKnowledgeProps, y as RegisterAiToolProps } from './room-C6NTdu0X.cjs';
export { A as AiChatStatus, C as ClientContext, L as LiveblocksProvider, M as MutationContext, B as RoomContext, P as RoomProvider, S as SendAiMessageOptions, U as UseSendAiMessageOptions, z as UseThreadsOptions, E as createLiveblocksContext, O as createRoomContext, Q as useAddReaction, aT as useAiChat, aV as useAiChatMessages, aW as useAiChatStatus, aU as useAiChats, aF as useAttachmentUrl, T as useBroadcastEvent, V as useCanRedo, W as useCanUndo, D as useClient, aL as useCreateAiChat, X as useCreateComment, ay as useCreateFeed, aB as useCreateFeedMessage, Y as useCreateThread, aM as useDeleteAiChat, F as useDeleteAllInboxNotifications, Z as useDeleteComment, az as useDeleteFeed, aC as useDeleteFeedMessage, H as useDeleteInboxNotification, $ as useDeleteThread, a0 as useEditComment, a2 as useEditCommentMetadata, a1 as useEditThreadMetadata, I as useErrorListener, a7 as useEventListener, ax as useFeedMessages, aw as useFeeds, aQ as useGroupInfo, a8 as useHistory, ao as useHistoryVersionData, aG as useHistoryVersions, _ as useInboxNotificationThread, aI as useInboxNotifications, a9 as useIsInsideRoom, aa as useLostConnectionListener, J as useMarkAllInboxNotificationsAsRead, K as useMarkInboxNotificationAsRead, ab as useMarkThreadAsRead, a3 as useMarkThreadAsResolved, a4 as useMarkThreadAsUnresolved, ac as useMutation, ad as useMyPresence, aJ as useNotificationSettings, ap as useOther, aq as useOthers, ar as useOthersConnectionIds, ae as useOthersListener, as as useOthersMapped, af as useRedo, ag as useRemoveReaction, ah as useRoom, aP as useRoomInfo, aH as useRoomSubscriptionSettings, aE as useSearchComments, at as useSelf, aN as useSendAiMessage, ai as useStatus, au as useStorage, aj as useStorageRoot, a5 as useSubscribeToThread, N as useSyncStatus, ak as useThreadSubscription, av as useThreads, al as useUndo, aR as useUnreadInboxNotificationsCount, a6 as useUnsubscribeFromThread, aD as useUpdateFeedMessage, aA as useUpdateFeedMetadata, am as useUpdateMyPresence, aK as useUpdateNotificationSettings, an as useUpdateRoomSubscriptionSettings, aX as useUrlMetadata, aS as useUser, aO as useUserThreads_experimental } from './room-C6NTdu0X.cjs';
export { Json, JsonObject, isNotificationChannelEnabled, shallow } from '@liveblocks/client';
import * as react_jsx_runtime from 'react/jsx-runtime';
import * as react from 'react';
import { ReactNode } from 'react';
import '@liveblocks/core';

type Props = {
    fallback: ReactNode;
    children: (() => ReactNode | undefined) | ReactNode | undefined;
};
/**
 * Almost like a normal <Suspense> component, except that for server-side
 * renders, the fallback will be used.
 *
 * The child props will have to be provided in a function, i.e. change:
 *
 *   <Suspense fallback={<Loading />}>
 *     <MyRealComponent a={1} />
 *   </Suspense>
 *
 * To:
 *
 *   <ClientSideSuspense fallback={<Loading />}>
 *     <MyRealComponent a={1} />
 *   </ClientSideSuspense>
 *
 */
declare function ClientSideSuspense(props: Props): react_jsx_runtime.JSX.Element;

/**
 * Make knowledge about your application state available to any AI used in
 * a chat or a one-off request.
 *
 * For example:
 *
 *     <RegisterAiKnowledge
 *        description="The current mode of my application"
 *        value="dark" />
 *
 * Or scoped to a specific chat:
 *
 *     <RegisterAiKnowledge
 *        description="The current list of todos"
 *        value={todos}
 *        chatId="chat-1234" />
 *
 * By mounting this component, the AI will get access to this knwoledge.
 * By unmounting this component, the AI will no longer have access to it.
 * It can choose to use or ignore this knowledge in its responses.
 */
declare const RegisterAiKnowledge: react.NamedExoticComponent<RegisterAiKnowledgeProps>;
/**
 * Make a tool available to your AI chat or a one-off request.
 * A tool is a piece of functionality that the AI can call to perform an action
 * or look up information on the user's behalf.
 *
 * Also, tools are used to render custom UIs for tool invocations, which are
 * embedded inside the AI chat.
 *
 * For example:
 *
 *   <RegisterAiTool
 *     name="list-todos"
 *     tool={defineAiTool()({ ... })}
 *     />
 *
 * Or scoped to a specific chat:
 *
 *   <RegisterAiTool
 *     name="list-todos"
 *     tool={defineAiTool()({ ... })}
 *     chatId="chat-1234"
 *     />
 *
 * By mounting this component, the tool is made available.
 * By unmounting this component, the tool will no longer be available.
 */
declare const RegisterAiTool: react.NamedExoticComponent<RegisterAiToolProps>;

export { ClientSideSuspense, RegisterAiKnowledge, RegisterAiKnowledgeProps, RegisterAiTool, RegisterAiToolProps };
